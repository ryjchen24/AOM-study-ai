# Base
fastapi
uvicorn[standard]
python-dotenv
httpx
prisma

# Auth
itsdangerous   # signs the session cookie (used by SessionMiddleware + Authlib state)
authlib        # OAuth 2.0 / OIDC client for Google; pulls in cryptography to
               # verify the id_token signature (cryptography reused for key encryption)

# BYOK
cryptography   # Fernet (AES-128-CBC + HMAC) for KEY_ENCRYPTION_KEY
slowapi        # per-user rate limiting on /api/chat (keyed by user id)
